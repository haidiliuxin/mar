# Markov 核心代码说明

`markov_model.py` 负责加权训练、add-0.01 平滑、end-symbol 和长度分布归一化，以及单个口令的概率计算。代码把 d 阶定义为使用最近 d 个字符作为上下文，句首补 d 个 `START`。

`candidate_generator.py` 按概率阈值深度优先生成候选。第一版只使用“已经累计的负对数概率不能超过阈值”这一条安全剪枝，方便和穷举结果比较。

## 两种正规化

end-symbol 模型在长度达到最短长度后允许生成 `END`。长度小于最短长度时，`END` 不在当前输出空间中，分母只包含字符转移计数和字符的平滑质量：

```text
P(c|h, length < min_length)
= (C(h,c)+delta) / (字符转移计数总和+delta*字符数)
```

达到最短长度、尚未达到最大长度后，字符和 `END` 共同构成输出空间：

```text
P(x|h, min_length <= length < max_length)
= (C(h,x)+delta) / (字符及END计数总和+delta*(字符数+1))
```

达到最大长度时强制结束，`P(END)=1`。这样非法短口令不会取得概率，全部合法口令的概率总和仍为 1。

distribution 模型不训练 `END` 转移。字符 Markov 概率只在字符表上归一化，完整口令概率再乘经验长度概率：

```text
P(password) = P(length) * 连乘 P(character|context)
```

实际计算使用 `-log2 P(password)`，避免很多小概率连续相乘造成浮点下溢。

